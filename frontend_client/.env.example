# PUBLIC_INTERFACE
# Environment variables needed by the frontend_client React app

# Base URL for the backend API (e.g., http://localhost:3001)
# If not set, the app falls back to /api (and dev proxy will route to http://localhost:3001)
REACT_APP_API_BASE_URL=
